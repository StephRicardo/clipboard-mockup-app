<%= form_for @design, html: { multipart: true } do |f| %>
  <% if design.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(design.errors.count, "error") %> prohibited this design from being saved:</h2>

      <ul>
      <% design.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title, id: :design_title %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image, id: :design_image %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price, id: :design_price %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description, id: :design_description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
