<!-- TODO Update Heart to Star, embed font awesome star icon. Step 7 in tutorial  -->

<i class="fa fa-star" aria-hidden="true" aria-label="star"></i>

<% if current_user.star?(design) %>
  <%= button_to "Unheart", unheart_path(post_id: post.id), remote: true, method: :delete  %>
<% else %>
  <%= button_to "Heart", heart_path(post_id: post.id), remote: true  %>
<% end %>
